services:
  theonefile_verse:
    image: ghcr.io/gelatinescreams/theonefile_verse:latest
    ports:
      - "10101:10101"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=10101
      - DATA_DIR=/app/data
      - UPDATE_INTERVAL=24
    restart: unless-stopped
